# ===============================================
# VARIÁVEIS DE AMBIENTE - PROJETO LOVABLE CLOUD
# ===============================================
#
# IMPORTANTE: Este projeto usa Lovable Cloud (Supabase).
# As variáveis VITE_SUPABASE_* são gerenciadas automaticamente.
# NUNCA edite o arquivo .env manualmente!
#
# Para desenvolvimento local:
# 1. Copie este arquivo: cp .env.example .env
# 2. O Lovable preencherá automaticamente as chaves do Supabase
# 3. Configure apenas as variáveis de terceiros abaixo
# ===============================================

# -----------------
# SUPABASE (Lovable Cloud) - NÃO EDITAR
# -----------------
# Gerenciado automaticamente pelo Lovable
VITE_SUPABASE_PROJECT_ID=__auto_generated__
VITE_SUPABASE_PUBLISHABLE_KEY=__auto_generated__
VITE_SUPABASE_URL=__auto_generated__

# -----------------
# STRIPE (Configurado nos Secrets do Supabase)
# -----------------
# Use o painel do Lovable Cloud para configurar:
# Settings > Secrets > Add Secret
STRIPE_SECRET_KEY=__set_in_lovable_secrets__
STRIPE_WEBHOOK_SECRET=__set_in_lovable_secrets__

# -----------------
# OUTRAS VARIÁVEIS (se necessário no futuro)
# -----------------
# NODE_ENV=development
# VITE_APP_VERSION=1.0.0

# ===============================================
# SEGURANÇA: NUNCA COMITAR VALORES REAIS!
# ===============================================
